name: Daily Environmental ETL

on:
  schedule:
    - cron: '0 0 * * *' # Runs at midnight UTC every day
  workflow_dispatch: # Allows manual trigger

jobs:
  extract_and_merge:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas geopy requests

    - name: Run OpenAQ Extractor (Layer 1 & 2)
      run: python src/pipeline/extractors/openaq_extractor.py

    - name: Run Indicator Generator (Layer 3 & 4)
      run: python src/pipeline/transformers/indicator_generator.py

    - name: Run Dataset Merger (Layer 5)
      run: python src/pipeline/merger.py

    - name: Commit and push changes
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git add data/
        git commit -m "Automated Daily ETL Update" || echo "No changes to commit"
        git push
